

# Crearse cuenta en tailscale
# Instalar tailscale en vm
curl -fsSL https://tailscale.com/install.sh | sh
# Loguearse con link y cuenta creada
# Generate auth key
sudo tailscale up --ssh --authkey=<your-auth-key> --accept-dns=false
